// const fullName = (fn,ln) => {
//     console.log(`${fn} ${ln}`);
    
//     return `${fn} ${ln}`
// }

// fullName("preston", "mayieka")


const addNumbers = (x,y) => {
    console.log(x + y);
    
    return x + y
}

addNumbers(3,4)